name: liquid_glass_nav_bar_example
description: Demonstrates how to use the liquid_glass_nav_bar package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  liquid_glass_nav_bar:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
